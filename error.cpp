#include <iostream>
#include <exception>

using namespace std;

int single_character_validation (char input) {
// This makes sure the user has entered a lowercase single character that is a letter, not a number.
// This function returns 0 if the letter passes validation, and returns 1 if the letter does not.
// The function falls back on returning 2 if it can't return 1 or 0.



    return 2;
}
